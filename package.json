{
  "name": "start-cookie",
  "version": "0.0.0",
  "description": "Tiny isomorphic cookie helper for TanStack Start.",
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "files": ["dist"],
  "scripts": {
    "build": "tsdown",
    "check": "tsdown --attw --publint",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@tanstack/react-start": ">=1.168.0"
  },
  "devDependencies": {
    "@tanstack/react-start": "^1.168.0",
    "@types/bun": "latest",
    "tsdown": "^0.9.0",
    "typescript": "^5.6.0"
  }
}
